<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.ssm.mapper.LoginMapper">


    <select id="selectUidByLoginAccount" resultType="User">
        select user_id userId,
               user_name userName,
               login_account loginAccount,
               login_password loginPassword from project_user where login_account=#{loginAccount} and login_password=#{LoginPassword}
    </select>
</mapper>